#include "main.h"
/**
 * add -  adds two integers and returns the result.
 *
 * @a: first integer number.
 * @b: second integer number.
 *
 * Return: a + b.
 */
int add(int a, int b)
{
	return (a + b);
}
